import sys

options = sys.argv #guardaremos todas la opciones enviadas por parametro

if '-a' in options:
    print("ejecutando el comando -a del parametro")
    
if '-b' in options:
    print("ejecutando el comando -b del parametro")    
    
  