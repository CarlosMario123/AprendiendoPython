import sys

print(sys.argv)#si por parametro en la consola enviamos asi nomas nos va dar un arreglo con la ruta
#pero si ejecutamos con mas parametro por ejemplo asi
#python ruta  -a -b n... -->dado que a y b son parametro de ejemplo que se puede pasar sys.argv leera los parametros enviado

#que uso te preguntaras le podemos dar esto esto puede ser muy util al crear scripts con datos personalizados
#ejemplo de uso en el documento example1 en carpeta native